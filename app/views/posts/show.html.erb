<% provide(:title, @post.title) %>

<p>
  Title: <%= @post.title %>
</p>

<p>
  Content: <%= @post.content %>
</p>

<div id="post-<%= @post.id %>">
    <%= render partial: 'stocks/stock', locals: { post: @post } %>
</div>
