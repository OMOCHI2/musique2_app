<% provide :title, "お問い合わせ" %>
<div class="container mt-4">
  <div class="row">
    <div class="col-md-9 m-auto">
      <h2>お問い合わせ</h2>
      <%= form_with model: @contact, local: true, url: contact_done_path do |form| %>

        <div class="form-group">
          <%= form.label :name, "名前" %>
          <%= form.text_field :name, value: @contact.name, id: "name", class: "form-control-plaintext" %>
        </div>

        <div class="form-group">
          <%= form.label :email, "メールアドレス" %>
          <%= form.text_field :email, value: @contact.email, id: "email", class: "form-control-plaintext" %>
        </div>

        <div class="form-group">
          <%= form.label :content, "お問い合わせ内容" %>
          <%= form.text_area :content, value: @contact.content, id: "content", class: "form-control-plaintext", rows: "10" %>
        </div>

        <p>この内容で送信します。よろしいですか？</p>

        <div class="float-end">
          <%= form.submit "戻る", name: "back", class: "btn btn-dark" %>
          <%= form.submit "送信", class: "btn btn-secondary" %>
        </div>
      <% end %>

    </div>
  </div>
</div>
