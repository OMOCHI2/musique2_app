<div class="center jumbotron">
  <h1>Welcome to the Musique</h1>

  <h2>
    This is the home page for the
    <a href="https://railstutorial.jp/">DTM</a>
    sample application.
  </h2>

  <%= link_to "新規登録", signup_path, class: "btn btn-lg btn-primary" %>
</div>
<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <div>
        <h4>カテゴリー</h4>
        <% @categories.each do |category| %>
          <%= link_to categories_path(name: category.name), class: "me-3" do %>
            <i class="fas fa-tag"></i>
            <%= category.name %>
          <% end %>
        <% end %>
      </div>

    </aside>
    <div class="col-md-8">
      <% if logged_in? %>
        <h3>
          フォローしているユーザーの記事
          <small class="ms-3">(新着順 10件)</small>
        </h3>
        <% if @following_users_posts.any? %>
          <ol class="list-unstyled">
            <%= render "shared/following_users_post" %>
          </ol>
        <% else %>
          <hr class="my-5">
          <p>
            フォローしているユーザーはまだいません。
            興味のある記事の投稿者を積極的にフォローしてみよう！
          </p>
        <% end %>
      <% end %>

      <h3>ストック数の多い記事</h3>
        <ol class="list-unstyled">
        <%= render "shared/following_users_post" %>
        </ol>
          <hr class="my-5">
        <p>
          フォローしているユーザーはまだいません。
          興味のある記事の投稿者を積極的にフォローしてみよう！
        </p>
    </div>
  </div>
<% end %>
