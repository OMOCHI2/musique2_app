<% provide :title, "お問い合わせ" %>
<div class="container mt-4">
  <div class="row">
    <div class="col-md-9 m-auto">
      <h2>お問い合わせ</h2>
      <%= form_with model: @contact, url: contact_done_path do |f| %>

        <div class="form-group">
          <%= f.label :name, "名前" %>
          <%= f.text_field :name, value: @contact.name, id: "name", class: "form-control-plaintext" %>
        </div>

        <div class="form-group">
          <%= f.label :email, "メールアドレス" %>
          <%= f.text_field :email, value: @contact.email, id: "email", class: "form-control-plaintext" %>
        </div>

        <div class="form-group">
          <%= f.label :content, "お問い合わせ内容" %>
          <%= f.text_area :content, value: @contact.content, id: "content", class: "form-control-plaintext", rows: "10" %>
        </div>

        <p>この内容で送信します。よろしいですか？</p>

        <div class="float-end">
          <%= f.submit "戻る", name: "back", class: "btn btn-dark" %>
          <%= f.submit "送信", class: "btn btn-secondary" %>
        </div>
      <% end %>

    </div>
  </div>
</div>
